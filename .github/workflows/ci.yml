name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install locked dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-lock.txt
          python -m pip install -e . --no-deps

      - name: Ruff
        run: python -m ruff check .

      - name: Type check
        run: python -m mypy src/swarmgov

      - name: Pytest with coverage
        run: |
          python -m coverage run -m pytest
          python -m coverage report --show-missing --fail-under=0

      - name: Confirmatory canary pipeline
        run: |
          python experiments/scripts/run_confirmatory_canary_pipeline.py \
            --output-root /tmp/swarmgov-ci-canary \
            --max-runs 2
